.row
  .col-sm-10
    %p.expectation-row{class: 'bg-'+status_class_for(exp)}
      =# fa_icon(exp.matcher.icon, 'fa-lg fa-fw')
      %strong= exp.subject
      %span.text-semi-muted should
      = exp.matcher
      %strong= exp.expected
  .col-sm-2
    .pull-right
      = subtle_button(icon_class: delete_icon_class, href: exp, method: :delete)
