.row
  .col-sm-offset-3.col-sm-6
    .page-header
      %h1.text-center
        = "#{passed_icon} #{@user_input} is up".html_safe

      %p
        We tested #{@user_input} for a <code>200</code> response, following
        up to four redirects.

      %p 
        %button.btn.btn-default.btn-lg.btn-block.btn-success
          Save this test
